### OpenAI Assistant

- create python venv
  - `python3 -m venv myenv`
- active the env
  - `source myenv/bin/activate`
- install the requirments
  `pip install -r requirements.txt`
- to run
  `python3 main.py`